<div class="container">
<section>
	<center><h1>So, you want to be a superhero?...</h1></center>
	<center><p>...or at least a PHP Code Hero. Well, we have no radio-active spiders or kryptonite here but if you follow our Lessons and finish the Challenges, you'll be practicing your PHP powers in no time!</p></center>
	<div>
	<h2>Levels</h2>
	<hr>
	<ul>
		<% @levels.each do |level| %>
			<li><div><%= level.title %></div></li>
		<% end %>
	</ul>
	</div>
</section>
<aside>
	<div class="aside_form">
  	<% if current_user %>
    	<%= link_to "View or Edit your Account", current_user %><br><br>
    	<%= link_to "Logout", session_path, method: :delete %>
  	<% else %>
  		<h2>Sign In</h2>
				<%= form_tag(session_path) do %>
				<p>
				<%= label_tag :email %><br>
				<%= email_field_tag :email, nil %>
				<br>
				<br>
				<%= label_tag :password %><br>
				<%= password_field_tag :password, nil %>
				<br>
				<br>
				<%= submit_tag "Sign In" %>
				</p>
				<% end %>
    	<p>Don't have an account? <%= link_to "Sign Up", signup_path %></p>
		<% end %>

  	<% flash.each do |key, value| %>
    <%= content_tag(:p, value) %>

  	<% end %>
	</div>
	<div class="aside_users">
		<h2>All Users</h2>
		<hr>
		<ul>
			<% @users.each do |user| %>
				<li>
					<%= image_tag user.image_url%>
					<%= user.name %>
					Level <%= user.level_id %>
				</li>
				<hr>

			<% end %>
		</ul>
	</div>
</aside>
</div>
