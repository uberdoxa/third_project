<div class="container">
<section>
	<h1>So, you want to be a superhero?...</h1>
	<p>...or at least a PHP Code Hero. Well, we have no radio-active spiders or kryptonite here but if you follow our Lessons and finish the Challenges, you'll be practicing your PHP powers in no time!</p>
	<h2>Levels</h2>
	<ul>
		<% @levels.each do |level| %>
			<li><div><%= level.title %></div></li>
		<% end %>
	</ul>
</section>
<aside>
	<div>
  	<% if current_user %>
    	<%= link_to "View or Edit your Account", current_user %>
    	<%= link_to "Logout", session_path, method: :delete %>
  	<% else %>
  		<h2>Sign In</h2>
				<%= form_tag(session_path) do %>
				<p>
				<%= label_tag :email %>
				<%= email_field_tag :email, nil %>
				</p>
				<p>
				<%= label_tag :password %>
				<%= password_field_tag :password, nil %>
				</p>
				<p>
				<%= submit_tag "Sign In" %>
				</p>
				<% end %>
    	<p>Don't have an account? <%= link_to "Sign Up", signup_path %></p>
		<% end %>

  	<% flash.each do |key, value| %>
    <%= content_tag(:p, value) %>

  	<% end %>
	</div>
	<div>
		<h1>All Users</h1>
		<ul>
			<% @users.each do |user| %>
				<li>
					<%= user.name %>
				</li>
			<% end %>
		</ul>
	</div>
</aside>
</div>
