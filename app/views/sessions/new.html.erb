<div class="container">
<section>
	<% if current_user %>
		<center><h1>Continue your code journey!</h1></center>
		<center><p>Your PHP powers won't develop themselves! Click on a lesson below to accept your challenge and level up to PHP Code Hero!</p></center>
	<% else %>
		<center><h1>So, you want to be a superhero?...</h1></center>
		<center  id='monoL1'><p>...or at least a PHP Code Hero. Well, we have no radio-active spiders or kryptonite here but if you follow our Lessons and finish the Challenges, you'll be practicing your PHP powers in no time!</p></center>
	  <center  id='monoL2'><p>I am sure you will agree acquiring the power to write a Hello World program, or declaring a variable and storing a value to it. Will be far more useful than the ability to leap a tall buildings in a single bound</p></center>
	  <center  id='monoL3'><p></p></center>

	<% end %>
	<div>
	<h2>Levels</h2>
	<hr>
	<ul>
		<% @levels.each do |level| %>
			<li><a href="levels/<%= level.id %>"><div><%= level.title %></div></a></li>
		<% end %>
	</ul>
	</div>
</section>
<aside>
	<div class="aside_form">
  	<% if current_user %>
    	<%= link_to "View or Edit your Account", current_user %><br><br>
    	<%= link_to "Logout", session_path, method: :delete %><br><br>
  	<% else %>
  		<h2>Sign In</h2>
				<%= form_tag(session_path) do %>
				<p>
				<%= label_tag :email %><br>
				<%= email_field_tag :email, nil %>
				<br>
				<br>
				<%= label_tag :password %><br>
				<%= password_field_tag :password, nil %>
				<br>
				<br>
				<%= submit_tag "Sign In", :id=>'signin_button' %>
				</p>
				<% end %>
    	<p>Don't have an account? <%= link_to "Sign Up", signup_path %></p>
		<% end %>

  	<% flash.each do |key, value| %>
    <%= content_tag(:p, value) %>

  	<% end %>
	</div>
	<div class="aside_users">
		<h2>All Users</h2>
		<hr>
		<ul>
			<% @users.each do |user| %>
				<li>
					<%= image_tag user.image_url%>
					<%= user.name %>:
					Level <%= user.level_id %>
				</li>
				<hr>

			<% end %>
		</ul>
	</div>
</aside>
<div style="clear: both">&nbsp;</div>
</div>
<div style="clear: both" />
