<div class="showcontainer">
<h2><%= @user.name %></h2>
<p><%= image_tag @user.image_url.to_s %>
<p><%= @user.email %></p>
<%= link_to "Edit Account", edit_user_path(@user) %>
<%= link_to "Delete user", user_path(@user.id), method: :delete,
													 data: { confirm: "Are you sure?" } %>
</div>
